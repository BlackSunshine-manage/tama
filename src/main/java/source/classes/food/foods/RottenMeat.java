package source.classes.food.foods;

import javafx.scene.image.ImageView;
import source.classes.food.Food;

public class RottenMeat extends Food {
    public RottenMeat(ImageView imageView, int x, int y) {
        super(imageView, x, y);
    }
}
